version: '3.8'
services:
  db:
    image: postgres:15.3
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=VLAD753
    ports:
      - '5432:5432'

#docker compose up
#docker compose up ...